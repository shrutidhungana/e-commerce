import React from 'react';

type headerProps = {
    
};

const Header:React.FC<headerProps> = () => {
    
    return (
        <div>
           Header Component
        </div>
    );
}
export default Header;
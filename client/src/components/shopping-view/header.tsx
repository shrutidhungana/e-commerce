import React from 'react';

type headerProps = {
    
};

const ShoppingHeader:React.FC<headerProps> = () => {
    
    return (
        <div>
            Shopping Header
        </div>
    );
}
export default ShoppingHeader;